import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ionic-1st-App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;

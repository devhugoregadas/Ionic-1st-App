import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter', //Domain name & App identifier
  appName: 'Ionic-1st-App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
